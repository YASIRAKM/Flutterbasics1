import 'package:flutter/material.dart';

class Peoplenear extends StatelessWidget {
  const Peoplenear({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("PEOPLE NEAR BY:"),
    );
  }
}
