import 'package:flutter/material.dart';

class Contatcs extends StatelessWidget {
  const Contatcs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("contact"),
    );
  }
}
